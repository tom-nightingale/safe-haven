import { Literata, Inter } from "next/font/google";

export const literataSerif = Literata({
  variable: "--font-literata",
  subsets: ["latin"],
});

export const interSans = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});
